# Electron + reactjs + redux starter

electron, reactjs, redux starter kit using typescript

- install:

```
  yarn install
```

- start dev:

```
  yarn dev
```

- start prod:

```
  yarn start
```

- packaging:

```
  yarn pack:exe
```
